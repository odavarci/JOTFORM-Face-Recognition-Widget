import axios from "axios";

const Demo = (props) => {
    let a = {first:"ömer", last:"davarcı"};

    console.log(a.first);

    return(
        <div>
            <h1>Hello World</h1>

        </div>
    );
}

export default Demo;